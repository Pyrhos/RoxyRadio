{
  "name": "rourin-radio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "vitest",
    "build": "vite build",
    "preview": "vite preview",
    "data:fetch": "node scripts/fetch-dates.js",
    "data:harvest": "node scripts/harvest.js",
    "data:reorder": "node scripts/reorder.js",
    "data:sync": "node scripts/harvest.js && node scripts/fetch-dates.js && node scripts/reorder.js"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.13",
    "happy-dom": "^20.0.10",
    "vite": "^7.2.4",
    "vite-plugin-compression": "^0.5.1",
    "vitest": "^4.0.13"
  },
  "dependencies": {
    "fuse.js": "^7.1.0"
  }
}
